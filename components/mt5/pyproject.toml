[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "trademachine-mt5"
version = "0.1.0"
description = "MT5 Utilities component"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
include = ["trademachine*"]
