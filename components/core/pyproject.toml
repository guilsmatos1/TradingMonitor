[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "trademachine-core"
version = "0.1.0"
description = "Core utilities component"
dependencies = ["numpy>=1.26"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["trademachine*"]
